# Original prototype

These are the five static HTML files from the original Tempo build, kept
untouched for reference. Nothing in the new app depends on them.

The design language here — the navy/green palette, the glass cards, the gradient
mesh background, the floating-label forms — was ported into the Next.js app in
`src/app/globals.css`. Same colours, same feel, now as a real design system.
